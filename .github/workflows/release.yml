name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write      # для коммитов и тегов
  id-token: write      # для Trusted Publishing (npm)

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # обязательно для тегов

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org/

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Generate release notes from commits
        id: notes
        run: |
          set -e

          PREV_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          if [ -z "$PREV_TAG" ]; then
            COMMITS=$(git log --pretty=format:"- %s")
          else
            COMMITS=$(git log "$PREV_TAG"..HEAD --pretty=format:"- %s")
          fi

          echo "RELEASE_NOTES<<EOF" >> $GITHUB_ENV
          echo "$COMMITS" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      - name: Bump patch version and create annotated tag
        run: |
          npm version patch -m "Release %s%n%n${RELEASE_NOTES}"
        env:
          GIT_AUTHOR_NAME: github-actions
          GIT_AUTHOR_EMAIL: github-actions@github.com
          GIT_COMMITTER_NAME: github-actions
          GIT_COMMITTER_EMAIL: github-actions@github.com
      - name: Cleanup old npm auth
        run: |
          rm -f ~/.npmrc
          npm config delete //registry.npmjs.org/:_authToken || true
          
      - name: Publish to npm (Trusted Publishing)
        run: npm publish

      - name: Push commits and tags
        run: git push --follow-tags
